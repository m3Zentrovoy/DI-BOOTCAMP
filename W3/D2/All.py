# Ключевые факты теории из лекции

# 1. Structured vs Unstructured Data
# Structured Data:

# Организованные данные в таблицах (например, Excel, SQL).
# Легко искать и обрабатывать.
# Пример: база данных клиентов (имя, возраст, покупки).
# Unstructured Data:
# Неорганизованные данные (текст, видео, изображения).
# Сложно обрабатывать, нужны методы вроде NLP.
# Пример: твиты, отзывы, фото.
# Проблемы с неструктурированными данными:
# Сложности обработки, хранения, извлечения.
# Требуются продвинутые техники (например, обработка текста).
# Зачем это важно:
# Разные данные — разные подходы: pandas для структурированных, NLP для неструктурированных.

# 2. Importing Data
# Что такое импорт данных:

# Загрузка данных в Python из внешних источников (CSV, JSON, Excel).
# Форматы и методы:
# CSV: pd.read_csv('file.csv') — для табличных данных.
# JSON: pd.read_json('file.json') — для вложенных структур.
# Excel: pd.read_excel('file.xlsx') — для таблиц с листами.
# Colab и Google Drive:
# Подключи Drive: from google.colab import drive; drive.mount('/content/drive').
# Импортируй: pd.read_csv('/content/drive/My Drive/file.csv').
# Зачем это важно:
# Импорт — первый шаг для анализа данных.
# pandas универсален: поддерживает разные форматы.

# 3. Exporting Data
# Что такое экспорт данных:

# Сохранение данных из Python в файлы (CSV, JSON, Excel).
# Форматы и методы:
# CSV: df.to_csv('file.csv', index=False) — для таблиц.
# JSON: df.to_json('file.json') — для вложенных данных.
# orient (split, records, и т.д.) меняет структуру JSON.
# Excel: df.to_excel('file.xlsx') — для таблиц с листами.
# Настройки:
# index=False убирает индекс.
# Чанки (chunksize) или Parquet для больших данных.
# Зачем это важно:
# Экспорт нужен для сохранения результатов и обмена данными.
# Выбор формата зависит от цели (CSV для простоты, JSON для API).

# 4. Dataset Generation
# Что такое генерация данных:

# Создание синтетических данных или модификация существующих.
# Методы:
# Synthetic Data: Создание данных с faker (имена, даты) и numpy (числа).
# Пример: имена и случайные возраста.
# Data Augmentation: Увеличение данных (например, поворот изображений).
# Simulation-Based Generation: Симуляция сценариев (например, цены акций).
# Crowdsourcing: Сбор данных от людей (например, опросы).
# Лучшие практики:
# Данные должны быть разнообразными и реалистичными.
# Учитывай этику (не используй личные данные).
# Проверяй качество (без ошибок, предвзятости).
# Зачем это важно:
# Синтетические данные полезны, если реальных данных нет или они конфиденциальны.
# Генерация улучшает модели (например, через аугментацию).
# Итог
# Эти факты — основа лекции. Ты знаешь:

# Разницу между структурированными и неструктурированными данными.
# Как импортировать данные в Python (CSV, JSON, Excel, через Google Drive).
# Как экспортировать данные (CSV, JSON, Excel) и настраивать параметры.
# Методы генерации данных (синтетические, аугментация, симуляция, краудсорсинг) и лучшие практики.
# Теперь ты готов к практике с заданием! Если нужно что-то уточнить, дай знать. 😊