URL: https://github.com/m3Zentrovoy/DI-BOOTCAMP/blob/main/W7/D1/XP_W7-D1.sql
suggestions for improvement:
- In Exercise 1, the code uses `TO_CHAR` which is a PostgreSQL function.  SQLite uses `STRFTIME` for date formatting.  The `||` concatenation operator is correct for both databases.
- Exercise 1 is missing a crucial step: the creation of the `emp_dataset` temporary table before creating `df_employee`. The code should explicitly show the `CREATE TABLE emp_dataset AS SELECT ... FROM ...` statement, combining data from the four tables (companies, employees, functions, salaries) using `LEFT JOIN` as specified in Exercise 1.
- In Exercise 2, while the `TRIM` function is used correctly, there's room for improvement. It should be applied to *all* text columns, not just the ones explicitly listed. A more efficient approach might involve dynamically identifying all text columns and applying the `TRIM` function programmatically.
- The deletion of rows with NULL values in Exercise 2 could lead to data loss.  It's usually better to handle missing values by imputing them (using the average, median, mode, etc.) rather than removing them entirely.  Consider adding a note about this decision.
- In Exercise 4, the query calculating the percentage of employees per city has a potential error. The `SUM(COUNT(*)) OVER ()` part will result in a total count across all rows, not grouped by `company_city`, which is likely not the intended behavior. The percentage should be calculated within each `company_city` group. The calculation `COUNT(*) * 100.0 / SUM(COUNT(*)) OVER ()` is correct in principle but needs the correct grouping.
- The date handling in some queries (particularly Exercise 5's monthly average employee count by function group) needs attention.  Explicitly convert the `date` column to a `DATE` type for consistent and reliable date operations. The current `TO_CHAR(TO_DATE(s.date::text, 'YYYY-DD-MM'), 'YYYY-MM')` is brittle and depends on the exact format of the date string. It should be improved to use the `STRFTIME` function correctly and consistently.
- The query in Exercise 5 for minimum/maximum employees is good, but adding a `date` column to the result for clarity would make it even better.
Brief justification:
- correctness: The SQL code attempts to address all the exercises. However, there are several inconsistencies and potential errors (see feedback section).  The exercise involving `LEFT JOIN` is mostly done correctly, but missing the initial table creation. The data cleaning steps are partially correct, but the approach for handling missing values is overly simplistic.  Some aggregations are correct, but others have minor flaws or inefficiencies.  Overall, the correctness is high (90%) because it correctly follows the spirit of the exercises, but it needs refinement to achieve full correctness.
- readability: The code is well-structured and uses clear variable names. The comments are helpful, though a more detailed explanation of complex logic would be beneficial (especially the potentially flawed percentage calculation in Exercise 4). The overall structure enhances readability.
- performance: The performance could be improved, particularly with regard to data cleaning.  The individual checks for NULLs in Exercise 2 could be made more efficient; using a `CASE` statement or `COALESCE` might improve it.  Some aggregations are correctly optimized, others are not.  Some inefficient date formatting also needs resolving.
- security: No security vulnerabilities are present in the provided SQL code snippets. They are purely focused on data manipulation and analysis, with no external inputs or sensitive data handling.

